query getTeamsFromTopFiveLeagues($leagueSlug: String!) {
  football {
    competition(slug: $leagueSlug) {
      clubs {
        nodes {
          slug
        }
        pageInfo {
          endCursor
          hasNextPage
        }
      }
    }
  }
}

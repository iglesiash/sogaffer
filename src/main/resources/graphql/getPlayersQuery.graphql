query getPlayersByTeam($teamSlug: String!) {
  football {
    club(slug: $teamSlug) {
      activePlayers {
        nodes {
          squaredPictureUrl
          slug
          displayName
        }
      }
    }
  }
}

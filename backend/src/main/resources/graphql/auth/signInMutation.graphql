mutation SignInMutation($input: signInInput!) {
    signIn(input: $input) {
        currentUser {
            slug
        }
        jwtToken(aud: "$aud") {
            token
            expiredAt
        }
        errors {
            message
        }
    }
}
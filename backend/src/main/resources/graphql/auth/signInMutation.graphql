mutation SignInMutation($input: signInInput!, $aud: String!) {
    signIn(input: $input) {
        currentUser {
            id
        }
        jwtToken(aud: $aud) {
            token
        }
        errors {
            message
        }
    }
}